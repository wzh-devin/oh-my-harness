---
name: debugging
description: 通过复现、调用链和最小验证定位根因。
---

# 故障排查

收集错误文本、实际输入、运行环境和最近改动。先从已有日志和最窄测试建立可复现失败，避免随意重启、清空数据或升级依赖。
检查报错入口、共享实现和所有关联调用方，提出一个可证伪假设；每次只改变一个相关因素。用证据解释异常为何产生，而非只添加吞错分支。
修复后运行能覆盖原始触发条件的检查以及直接受影响回归。明确记录尚未验证的环境。所有命令执行继续服从宿主审批。
