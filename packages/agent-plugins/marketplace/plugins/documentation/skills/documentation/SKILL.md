---
name: documentation
description: 根据当前代码整理可验证的使用说明和维护文档。
---

# 文档整理

先读取现有 README、构建脚本与用户指定模块，确认文档对象和读者。每个操作命令都必须来自当前项目；没有验证的命令标明待验证。
采用背景、前置条件、操作步骤、成功表现、故障排查的顺序。使用相对链接连接项目文件，不复制令牌和本机绝对路径。
只在用户要求的文件内编辑；没有写入授权时直接提供草稿。不自动提交代码或发布文档。
